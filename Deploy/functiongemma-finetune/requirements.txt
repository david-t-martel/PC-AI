# Core ML/Training (Pre-installed in Unsloth Docker, but listing for reference/local dev)
# unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
trl==0.8.6
# xformers

# Dependencies not in Unsloth base image
datasets>=2.16.0
pydantic>=2.0.0
typer>=0.9.0
rich>=13.0.0
wandb>=0.16.0

# Testing
pytest>=8.0.0
