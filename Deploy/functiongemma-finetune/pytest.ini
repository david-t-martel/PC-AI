[pytest]
addopts = -ra
markers =
    unit: fast unit tests
    integration: requires running services (vLLM/Docker)
    e2e: end-to-end tests that spin up local routers
    functional: validates PC_AI utilities from shell
