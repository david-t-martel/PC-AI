# PSSCRIPTANALYZER

Generated: 2026-01-27 05:57:41

## Warning
- Find-DuplicatesFast.ps1:271 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-DuplicatesFast.ps1:274 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-DuplicatesFast.ps1:275 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-DuplicatesFast.ps1:279 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-DuplicatesFast.ps1:280 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-DuplicatesFast.ps1:283 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-DuplicatesFast.ps1:122 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:150 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:158 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:163 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:166 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:171 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:194 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:227 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:228 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:229 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:230 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:231 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:232 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:233 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:234 [PSAvoidUsingWriteHost] File 'Find-DuplicatesFast.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicatesFast.ps1:118 [PSReviewUnusedParameter] The parameter 'ShowProgress' has been declared but not used.
- Find-DuplicatesFast.ps1:262 [PSUseSingularNouns] The cmdlet 'Find-WithFdForDuplicates' uses a plural noun. A singular noun should be used instead.
- Get-NetworkAdapters.ps1:27 [PSUseSingularNouns] The cmdlet 'Get-NetworkAdapters' uses a plural noun. A singular noun should be used instead.
- Get-ProcessesFast.ps1:148 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Get-ProcessesFast.ps1:152 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Get-ProcessesFast.ps1:153 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Get-ProcessesFast.ps1:158 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Get-ProcessesFast.ps1:163 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Get-ProcessesFast.ps1:168 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Get-ProcessesFast.ps1:256 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Get-ProcessesFast.ps1:144 [PSReviewUnusedParameter] The parameter 'RawOutput' has been declared but not used.
- Get-ProcessesFast.ps1:222 [PSReviewUnusedParameter] The parameter 'Tree' has been declared but not used.
- Get-ProcessesFast.ps1:223 [PSReviewUnusedParameter] The parameter 'Watch' has been declared but not used.
- Get-ProcessesFast.ps1:224 [PSReviewUnusedParameter] The parameter 'RawOutput' has been declared but not used.
- Get-ProcessesFast.ps1:136 [PSUseSingularNouns] The cmdlet 'Get-ProcessesWithProcs' uses a plural noun. A singular noun should be used instead.
- Search-ContentFast.ps1:222 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:226 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:227 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:232 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:237 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:242 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:247 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:248 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:253 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:254 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:259 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:262 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:267 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:270 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:271 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:415 [PSAvoidAssignmentToAutomaticVariable] The Variable 'matches' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-ContentFast.ps1:208 [PSReviewUnusedParameter] The parameter 'Pattern' has been declared but not used.
- Search-ContentFast.ps1:217 [PSReviewUnusedParameter] The parameter 'ThrottleLimit' has been declared but not used.
- Search-ContentFast.ps1:366 [PSReviewUnusedParameter] The parameter 'Pattern' has been declared but not used.
- Search-ContentFast.ps1:367 [PSReviewUnusedParameter] The parameter 'LiteralPattern' has been declared but not used.
- Search-ContentFast.ps1:371 [PSReviewUnusedParameter] The parameter 'WholeWord' has been declared but not used.
- Search-ContentFast.ps1:301 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Search-LogsFast.ps1:223 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Search-LogsFast.ps1:325 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Search-LogsFast.ps1:162 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:166 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:167 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:172 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:176 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:177 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:181 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:182 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:186 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:191 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:194 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:195 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Search-LogsFast.ps1:305 [PSAvoidAssignmentToAutomaticVariable] The Variable 'matches' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- DotNet-Helpers.ps1:99 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- DotNet-Helpers.ps1:19 [PSReviewUnusedParameter] The parameter 'Algorithm' has been declared but not used.
- DotNet-Helpers.ps1:77 [PSReviewUnusedParameter] The parameter 'ScriptBlock' has been declared but not used.
- Initialize-PcaiNative.ps1:167 [PSReviewUnusedParameter] The parameter 'IncludePattern' has been declared but not used.
- Initialize-PcaiNative.ps1:170 [PSReviewUnusedParameter] The parameter 'ExcludePattern' has been declared but not used.
- Compare-ToolPerformance.ps1:83 [PSAvoidUsingWriteHost] File 'Compare-ToolPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Compare-ToolPerformance.ps1:98 [PSAvoidUsingWriteHost] File 'Compare-ToolPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Compare-ToolPerformance.ps1:99 [PSAvoidUsingWriteHost] File 'Compare-ToolPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Compare-ToolPerformance.ps1:111 [PSAvoidUsingWriteHost] File 'Compare-ToolPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Compare-ToolPerformance.ps1:112 [PSAvoidUsingWriteHost] File 'Compare-ToolPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Compare-ToolPerformance.ps1:113 [PSAvoidUsingWriteHost] File 'Compare-ToolPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Compare-ToolPerformance.ps1:70 [PSReviewUnusedParameter] The parameter 'Detailed' has been declared but not used.
- Find-FilesFast.ps1:174 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:181 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:182 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:183 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:186 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:190 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:195 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:196 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:201 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:203 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:204 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:205 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:206 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:212 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:213 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:218 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:223 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:224 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:228 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:231 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Find-FilesFast.ps1:170 [PSReviewUnusedParameter] The parameter 'FullPath' has been declared but not used.
- Find-FilesFast.ps1:295 [PSReviewUnusedParameter] The parameter 'FullPath' has been declared but not used.
- Get-DiskUsageFast.ps1:174 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Get-DiskUsageFast.ps1:326 [PSUseApprovedVerbs] The cmdlet 'Sort-DiskUsageResults' uses an unapproved verb.
- Get-DiskUsageFast.ps1: [PSUseBOMForUnicodeEncodedFile] Missing BOM encoding for non-ASCII encoded file 'Get-DiskUsageFast.ps1'
- Get-DiskUsageFast.ps1:126 [PSUseSingularNouns] The cmdlet 'Convert-SizeToBytes' uses a plural noun. A singular noun should be used instead.
- Get-DiskUsageFast.ps1:326 [PSUseSingularNouns] The cmdlet 'Sort-DiskUsageResults' uses a plural noun. A singular noun should be used instead.
- Get-DiskUsageFast.ps1:363 [PSUseSingularNouns] The cmdlet 'Select-TopResults' uses a plural noun. A singular noun should be used instead.
- Measure-CommandPerformance.ps1:196 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Measure-CommandPerformance.ps1:109 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Measure-CommandPerformance.ps1:116 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Cleanup-Helpers.ps1:202 [PSUseSingularNouns] The cmdlet 'Test-PathExists' uses a plural noun. A singular noun should be used instead.
- Cleanup-Helpers.ps1:232 [PSUseSingularNouns] The cmdlet 'Get-TempPaths' uses a plural noun. A singular noun should be used instead.
- Clear-TempFiles.ps1:295 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Clear-TempFiles.ps1:266 [PSUseDeclaredVarsMoreThanAssignments] The variable 'actionDescription' is assigned but never used.
- Clear-TempFiles.ps1:204 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:205 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:206 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:207 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:208 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:211 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:214 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:220 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:303 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:305 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:308 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:311 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:325 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:326 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:327 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:328 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:329 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:330 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:333 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:334 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:336 [PSAvoidUsingWriteHost] File 'Clear-TempFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Clear-TempFiles.ps1:28 [PSUseSingularNouns] The cmdlet 'Clear-TempFiles' uses a plural noun. A singular noun should be used instead.
- Find-DuplicateFiles.ps1:298 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:299 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:300 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:301 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:302 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:303 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:304 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:305 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:306 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:307 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:310 [PSAvoidUsingWriteHost] File 'Find-DuplicateFiles.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Find-DuplicateFiles.ps1:37 [PSUseSingularNouns] The cmdlet 'Find-DuplicateFiles' uses a plural noun. A singular noun should be used instead.
- Get-PathDuplicates.ps1:93 [PSUseApprovedVerbs] The cmdlet 'Analyze-PathVariable' uses an unapproved verb.
- Get-PathDuplicates.ps1:19 [PSUseSingularNouns] The cmdlet 'Get-PathDuplicates' uses a plural noun. A singular noun should be used instead.
- Repair-MachinePath.ps1:244 [PSAvoidUsingWriteHost] File 'Repair-MachinePath.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-MachinePath.ps1:256 [PSAvoidUsingWriteHost] File 'Repair-MachinePath.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-MachinePath.ps1:257 [PSAvoidUsingWriteHost] File 'Repair-MachinePath.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-MachinePath.ps1:258 [PSAvoidUsingWriteHost] File 'Repair-MachinePath.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-MachinePath.ps1:280 [PSAvoidUsingWriteHost] File 'Repair-MachinePath.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-MachinePath.ps1:281 [PSAvoidUsingWriteHost] File 'Repair-MachinePath.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-MachinePath.ps1:282 [PSAvoidUsingWriteHost] File 'Repair-MachinePath.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-MachinePath.ps1:249 [PSUseDeclaredVarsMoreThanAssignments] The variable 'confirmMessage' is assigned but never used.
- Get-DeviceErrors.ps1:27 [PSUseSingularNouns] The cmdlet 'Get-DeviceErrors' uses a plural noun. A singular noun should be used instead.
- Get-SystemEvents.ps1:30 [PSUseSingularNouns] The cmdlet 'Get-SystemEvents' uses a plural noun. A singular noun should be used instead.
- New-DiagnosticReport.ps1:125 [PSAvoidUsingWriteHost] File 'New-DiagnosticReport.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- New-DiagnosticReport.ps1:214 [PSAvoidUsingWriteHost] File 'New-DiagnosticReport.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- New-DiagnosticReport.ps1:215 [PSAvoidUsingWriteHost] File 'New-DiagnosticReport.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- New-DiagnosticReport.ps1:216 [PSAvoidUsingWriteHost] File 'New-DiagnosticReport.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- New-DiagnosticReport.ps1:217 [PSAvoidUsingWriteHost] File 'New-DiagnosticReport.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- New-DiagnosticReport.ps1:218 [PSAvoidUsingWriteHost] File 'New-DiagnosticReport.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- New-DiagnosticReport.ps1:30 [PSUseShouldProcessForStateChangingFunctions] Function 'New-DiagnosticReport' has verb that could change system state. Therefore, the function has to support 'ShouldProcess'.
- USAGE_EXAMPLES.ps1:14 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:15 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:18 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:19 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:21 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:22 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:23 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:24 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:25 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:28 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:29 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:31 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:34 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:35 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:36 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:39 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:40 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:41 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:44 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:46 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:49 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:50 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:51 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:53 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:56 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:57 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:60 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:63 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:65 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:68 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:69 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:70 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:73 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:74 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:77 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:79 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:82 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:83 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:84 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:86 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:89 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:90 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:91 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:92 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:93 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:94 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:95 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:96 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:98 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- USAGE_EXAMPLES.ps1:99 [PSAvoidUsingWriteHost] File 'USAGE_EXAMPLES.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- LLM-Helpers.ps1:26 [PSUseDeclaredVarsMoreThanAssignments] The variable 'response' is assigned but never used.
- LLM-Helpers.ps1:53 [PSUseDeclaredVarsMoreThanAssignments] The variable 'response' is assigned but never used.
- LLM-Helpers.ps1:80 [PSUseDeclaredVarsMoreThanAssignments] The variable 'response' is assigned but never used.
- LLM-Helpers.ps1:255 [PSUseUsingScopeModifierInNewRunspaces] The variable '$u' is not declared within this ScriptBlock, and is missing the 'Using:' scope modifier.
- LLM-Helpers.ps1:255 [PSUseUsingScopeModifierInNewRunspaces] The variable '$bytes' is not declared within this ScriptBlock, and is missing the 'Using:' scope modifier.
- LLM-Helpers.ps1:255 [PSUseUsingScopeModifierInNewRunspaces] The variable '$hdrs' is not declared within this ScriptBlock, and is missing the 'Using:' scope modifier.
- LLM-Helpers.ps1:255 [PSUseUsingScopeModifierInNewRunspaces] The variable '$timeout' is not declared within this ScriptBlock, and is missing the 'Using:' scope modifier.
- LLM-Helpers.ps1:256 [PSUseUsingScopeModifierInNewRunspaces] The variable '$u' is not declared within this ScriptBlock, and is missing the 'Using:' scope modifier.
- LLM-Helpers.ps1:256 [PSUseUsingScopeModifierInNewRunspaces] The variable '$bytes' is not declared within this ScriptBlock, and is missing the 'Using:' scope modifier.
- LLM-Helpers.ps1:256 [PSUseUsingScopeModifierInNewRunspaces] The variable '$hdrs' is not declared within this ScriptBlock, and is missing the 'Using:' scope modifier.
- LLM-Helpers.ps1:256 [PSUseUsingScopeModifierInNewRunspaces] The variable '$timeout' is not declared within this ScriptBlock, and is missing the 'Using:' scope modifier.
- LLM-Helpers.ps1:291 [PSUseSingularNouns] The cmdlet 'Get-OllamaModels' uses a plural noun. A singular noun should be used instead.
- Invoke-FunctionGemmaReAct.ps1:300 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-FunctionGemmaReAct.ps1:302 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-FunctionGemmaReAct.ps1:302 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-FunctionGemmaReAct.ps1:304 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-FunctionGemmaReAct.ps1:221 [PSAvoidAssignmentToAutomaticVariable] The Variable 'Args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-FunctionGemmaReAct.ps1:115 [PSReviewUnusedParameter] The parameter 'TimeoutSeconds' has been declared but not used.
- Invoke-FunctionGemmaReAct.ps1:118 [PSReviewUnusedParameter] The parameter 'ShowProgress' has been declared but not used.
- Invoke-FunctionGemmaReAct.ps1:125 [PSReviewUnusedParameter] The parameter 'ProgressIntervalSeconds' has been declared but not used.
- Invoke-LLMChat.ps1:114 [PSAvoidUsingWriteHost] File 'Invoke-LLMChat.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-LLMChat.ps1:117 [PSAvoidUsingWriteHost] File 'Invoke-LLMChat.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-LLMChat.ps1:173 [PSAvoidUsingWriteHost] File 'Invoke-LLMChat.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-LLMChat.ps1:194 [PSAvoidUsingWriteHost] File 'Invoke-LLMChat.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-LLMChat.ps1:202 [PSAvoidUsingWriteHost] File 'Invoke-LLMChat.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-NativeSearch.ps1:551 [PSAvoidAssignmentToAutomaticVariable] The Variable 'matches' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-NativeSearch.ps1:564 [PSAvoidAssignmentToAutomaticVariable] The Variable 'matches' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-NativeSearch.ps1:426 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Invoke-NativeSearch.ps1:575 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Invoke-NativeSearch.ps1:544 [PSReviewUnusedParameter] The parameter 'ContextLines' has been declared but not used.
- Invoke-NativeSearch.ps1:399 [PSUseSingularNouns] The cmdlet 'Invoke-PowerShellDuplicates' uses a plural noun. A singular noun should be used instead.
- Invoke-PCDiagnosis.ps1:202 [PSAvoidUsingWriteHost] File 'Invoke-PCDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-PCDiagnosis.ps1:203 [PSAvoidUsingWriteHost] File 'Invoke-PCDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-PCDiagnosis.ps1:224 [PSAvoidUsingWriteHost] File 'Invoke-PCDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-PCDiagnosis.ps1:263 [PSAvoidUsingWriteHost] File 'Invoke-PCDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-PCDiagnosis.ps1:269 [PSAvoidUsingWriteHost] File 'Invoke-PCDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-PCDiagnosis.ps1:270 [PSAvoidUsingWriteHost] File 'Invoke-PCDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-PCDiagnosis.ps1:271 [PSAvoidUsingWriteHost] File 'Invoke-PCDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-PCDiagnosis.ps1:272 [PSAvoidUsingWriteHost] File 'Invoke-PCDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-PCDiagnosis.ps1:273 [PSAvoidUsingWriteHost] File 'Invoke-PCDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:95 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:102 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:108 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:113 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:117 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:132 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:135 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:165 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:183 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:200 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:246 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:249 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:257 [PSAvoidUsingWriteHost] File 'Invoke-SmartDiagnosis.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-SmartDiagnosis.ps1:91 [PSReviewUnusedParameter] The parameter 'TimeoutSeconds' has been declared but not used.
- Invoke-SmartDiagnosis.ps1:186 [PSUseDeclaredVarsMoreThanAssignments] The variable 'promptContext' is assigned but never used.
- Set-LLMConfig.ps1:131 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:142 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:150 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:151 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:155 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:156 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:159 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:173 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:178 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:184 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:190 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:196 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:202 [PSAvoidUsingWriteHost] File 'Set-LLMConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-LLMConfig.ps1:35 [PSUseShouldProcessForStateChangingFunctions] Function 'Set-LLMConfig' has verb that could change system state. Therefore, the function has to support 'ShouldProcess'.
- Set-LLMProviderOrder.ps1:36 [PSAvoidUsingWriteHost] File 'Set-LLMProviderOrder.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Network-Helpers.ps1:106 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Network-Helpers.ps1:62 [PSUseSingularNouns] The cmdlet 'Test-RegistryKeyExists' uses a plural noun. A singular noun should be used instead.
- Network-Helpers.ps1:218 [PSUseSingularNouns] The cmdlet 'Get-WSLDistributions' uses a plural noun. A singular noun should be used instead.
- Get-NetworkDiagnostics.ps1:42 [PSAvoidDefaultValueSwitchParameter] File 'Get-NetworkDiagnostics.ps1' has a switch parameter default to true.
- Get-NetworkDiagnostics.ps1:45 [PSAvoidDefaultValueSwitchParameter] File 'Get-NetworkDiagnostics.ps1' has a switch parameter default to true.
- Get-NetworkDiagnostics.ps1:48 [PSAvoidDefaultValueSwitchParameter] File 'Get-NetworkDiagnostics.ps1' has a switch parameter default to true.
- Get-NetworkDiagnostics.ps1:69 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:72 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:130 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:144 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:162 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:170 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:195 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:198 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:202 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:217 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:224 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:246 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:254 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:284 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:289 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:324 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:343 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:344 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:345 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:346 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:349 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:352 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:360 [PSAvoidUsingWriteHost] File 'Get-NetworkDiagnostics.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-NetworkDiagnostics.ps1:38 [PSUseSingularNouns] The cmdlet 'Get-NetworkDiagnostics' uses a plural noun. A singular noun should be used instead.
- Optimize-VSock.ps1:58 [PSAvoidAssignmentToAutomaticVariable] The Variable 'Profile' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Optimize-VSock.ps1:338 [PSAvoidUsingInvokeExpression] Invoke-Expression is used. Please remove Invoke-Expression from script and find other options instead.
- Optimize-VSock.ps1:103 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:111 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:115 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:124 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:229 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:252 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:260 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:275 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:293 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:297 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:302 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:307 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:312 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:340 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:343 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:347 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:355 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:364 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:367 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:371 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:378 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:379 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:380 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:381 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:384 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:388 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:392 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:396 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-VSock.ps1:397 [PSAvoidUsingWriteHost] File 'Optimize-VSock.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:79 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:82 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:93 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:94 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:107 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:111 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:122 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:132 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:150 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:160 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:163 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:178 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:188 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:192 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:208 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:217 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:221 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:225 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:241 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:251 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:270 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:286 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:290 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:296 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:316 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:319 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:324 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:330 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:347 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:350 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:366 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:371 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:383 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:394 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:402 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:407 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:433 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:434 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:435 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:436 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:439 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:442 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:450 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:454 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:455 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:462 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:463 [PSAvoidUsingWriteHost] File 'Test-WSLConnectivity.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Test-WSLConnectivity.ps1:63 [PSReviewUnusedParameter] The parameter 'Detailed' has been declared but not used.
- Watch-VSockPerformance.ps1:102 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Watch-VSockPerformance.ps1:198 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Watch-VSockPerformance.ps1:86 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:87 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:88 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:90 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:92 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:93 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:94 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:111 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:209 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:216 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:217 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:221 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:225 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:226 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:227 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:233 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:234 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:235 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:236 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:244 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:245 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:262 [PSAvoidUsingWriteHost] File 'Watch-VSockPerformance.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-VSockPerformance.ps1:65 [PSAvoidDefaultValueSwitchParameter] File 'Watch-VSockPerformance.ps1' has a switch parameter default to true.
- Performance-Helpers.ps1:210 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Performance-Helpers.ps1:252 [PSAvoidUsingWriteHost] File 'Performance-Helpers.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Performance-Helpers.ps1:255 [PSUseSingularNouns] The cmdlet 'Get-DiskIOCounters' uses a plural noun. A singular noun should be used instead.
- Get-ProcessPerformance.ps1:113 [PSAvoidDefaultValueSwitchParameter] File 'Get-ProcessPerformance.ps1' has a switch parameter default to true.
- Get-ProcessPerformance.ps1:114 [PSReviewUnusedParameter] The parameter 'IncludeSystemProcesses' has been declared but not used.
- Get-ProcessPerformance.ps1:181 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Get-ProcessPerformance.ps1:195 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Get-ProcessPerformance.ps1:207 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Optimize-Disks.ps1:25 [PSUseSingularNouns] The cmdlet 'Optimize-Disks' uses a plural noun. A singular noun should be used instead.
- Watch-SystemResources.ps1:297 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:298 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:303 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:304 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:305 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:310 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:311 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:312 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:313 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:316 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:317 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:318 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:321 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:322 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:323 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:327 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:336 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:339 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:347 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:354 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:366 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:383 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:384 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:391 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:392 [PSAvoidUsingWriteHost] File 'Watch-SystemResources.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Watch-SystemResources.ps1:143 [PSUseDeclaredVarsMoreThanAssignments] The variable 'processorCount' is assigned but never used.
- Watch-SystemResources.ps1:130 [PSReviewUnusedParameter] The parameter 'WarningThreshold' has been declared but not used.
- Watch-SystemResources.ps1:134 [PSReviewUnusedParameter] The parameter 'CriticalThreshold' has been declared but not used.
- Watch-SystemResources.ps1:34 [PSUseSingularNouns] The cmdlet 'Watch-SystemResources' uses a plural noun. A singular noun should be used instead.
- Dismount-UsbFromWSL.ps1:60 [PSAvoidUsingWriteHost] File 'Dismount-UsbFromWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Dismount-UsbFromWSL.ps1:66 [PSAvoidUsingWriteHost] File 'Dismount-UsbFromWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Dismount-UsbFromWSL.ps1:69 [PSAvoidUsingWriteHost] File 'Dismount-UsbFromWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Dismount-UsbFromWSL.ps1:74 [PSAvoidUsingWriteHost] File 'Dismount-UsbFromWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Dismount-UsbFromWSL.ps1:78 [PSAvoidUsingWriteHost] File 'Dismount-UsbFromWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:30 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:31 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:34 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:37 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:56 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:59 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:62 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:65 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:68 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:71 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:78 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:79 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:90 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:93 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:100 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-UsbWSLStatus.ps1:112 [PSAvoidUsingWriteHost] File 'Get-UsbWSLStatus.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-UsbBind.ps1:61 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-UsbBind.ps1:63 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-UsbBind.ps1:59 [PSAvoidUsingWriteHost] File 'Invoke-UsbBind.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-UsbBind.ps1:71 [PSAvoidUsingWriteHost] File 'Invoke-UsbBind.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Invoke-UsbBind.ps1:72 [PSAvoidUsingWriteHost] File 'Invoke-UsbBind.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Mount-UsbToWSL.ps1:70 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Mount-UsbToWSL.ps1:72 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Mount-UsbToWSL.ps1:68 [PSAvoidUsingWriteHost] File 'Mount-UsbToWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Mount-UsbToWSL.ps1:80 [PSAvoidUsingWriteHost] File 'Mount-UsbToWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Mount-UsbToWSL.ps1:83 [PSAvoidUsingWriteHost] File 'Mount-UsbToWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Mount-UsbToWSL.ps1:86 [PSAvoidUsingWriteHost] File 'Mount-UsbToWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Mount-UsbToWSL.ps1:87 [PSAvoidUsingWriteHost] File 'Mount-UsbToWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Mount-UsbToWSL.ps1:88 [PSAvoidUsingWriteHost] File 'Mount-UsbToWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Mount-UsbToWSL.ps1:89 [PSAvoidUsingWriteHost] File 'Mount-UsbToWSL.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Backup-WSLConfig.ps1:39 [PSAvoidUsingWriteHost] File 'Backup-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Enable-WSLSystemd.ps1:39 [PSAvoidDefaultValueSwitchParameter] File 'Enable-WSLSystemd.ps1' has a switch parameter default to true.
- Get-HVSockProxyStatus.ps1:45 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.
- Get-PcaiServiceHealth.ps1:88 [PSUseDeclaredVarsMoreThanAssignments] The variable 'info' is assigned but never used.
- Get-WSLEnvironmentHealth.ps1:68 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:71 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:82 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:93 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:101 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:113 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:127 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:148 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:149 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:150 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:151 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:152 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:153 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:156 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:159 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:160 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:161 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:164 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:165 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:168 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:173 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:174 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:235 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:241 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:349 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:355 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:537 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:542 [PSAvoidUsingWriteHost] File 'Get-WSLEnvironmentHealth.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Get-WSLEnvironmentHealth.ps1:421 [PSReviewUnusedParameter] The parameter 'AutoRecover' has been declared but not used.
- Get-WSLEnvironmentHealth.ps1:324 [PSUseDeclaredVarsMoreThanAssignments] The variable 'dockerInfo' is assigned but never used.
- Get-WSLEnvironmentHealth.ps1:623 [PSUseDeclaredVarsMoreThanAssignments] The variable 'dnsTest' is assigned but never used.
- Get-WSLEnvironmentHealth.ps1:636 [PSUseDeclaredVarsMoreThanAssignments] The variable 'pingTest' is assigned but never used.
- Install-WSLVsockBridge.ps1:68 [PSAvoidDefaultValueSwitchParameter] File 'Install-WSLVsockBridge.ps1' has a switch parameter default to true.
- Install-WSLVsockBridge.ps1:71 [PSAvoidDefaultValueSwitchParameter] File 'Install-WSLVsockBridge.ps1' has a switch parameter default to true.
- Install-WSLVsockBridge.ps1:98 [PSUseDeclaredVarsMoreThanAssignments] The variable 'socatCheck' is assigned but never used.
- Invoke-PcaiServiceHost.ps1:33 [PSAvoidAssignmentToAutomaticVariable] The Variable 'Args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLDockerHealthCheck.ps1:61 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLDockerHealthCheck.ps1:62 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLDockerHealthCheck.ps1:63 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLDockerHealthCheck.ps1:64 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:188 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:189 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:192 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:193 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:194 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:195 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:196 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:197 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:198 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:201 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:204 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:205 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:206 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:207 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:208 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:209 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:210 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:211 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Invoke-WSLNetworkToolkit.ps1:212 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Optimize-ModelHost.ps1:41 [PSAvoidUsingWriteHost] File 'Optimize-ModelHost.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-ModelHost.ps1:44 [PSAvoidUsingWriteHost] File 'Optimize-ModelHost.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-ModelHost.ps1:49 [PSAvoidUsingWriteHost] File 'Optimize-ModelHost.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-ModelHost.ps1:69 [PSAvoidUsingWriteHost] File 'Optimize-ModelHost.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-ModelHost.ps1:78 [PSAvoidUsingWriteHost] File 'Optimize-ModelHost.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-ModelHost.ps1:86 [PSAvoidUsingWriteHost] File 'Optimize-ModelHost.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:154 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:155 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:156 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:167 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:174 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:175 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:176 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:177 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:178 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:179 [PSAvoidUsingWriteHost] File 'Optimize-WSLConfig.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Optimize-WSLConfig.ps1:83 [PSUseApprovedVerbs] The cmdlet 'Normalize-WSLPath' uses an unapproved verb.
- Optimize-WSLConfig.ps1:52 [PSReviewUnusedParameter] The parameter 'Force' has been declared but not used.
- Repair-WSLNetworking.ps1:31 [PSAvoidDefaultValueSwitchParameter] File 'Repair-WSLNetworking.ps1' has a switch parameter default to true.
- Repair-WSLNetworking.ps1:35 [PSReviewUnusedParameter] The parameter 'Force' has been declared but not used.
- Repair-WSLNetworking.ps1:53 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:58 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:62 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:66 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:69 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:85 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:91 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:98 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:103 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:107 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:114 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:122 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:125 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:130 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:135 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:137 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Repair-WSLNetworking.ps1:140 [PSAvoidUsingWriteHost] File 'Repair-WSLNetworking.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-WSLDefenderExclusions.ps1:57 [PSAvoidUsingWriteHost] File 'Set-WSLDefenderExclusions.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-WSLDefenderExclusions.ps1:65 [PSAvoidUsingWriteHost] File 'Set-WSLDefenderExclusions.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-WSLDefenderExclusions.ps1:68 [PSAvoidUsingWriteHost] File 'Set-WSLDefenderExclusions.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-WSLDefenderExclusions.ps1:79 [PSAvoidUsingWriteHost] File 'Set-WSLDefenderExclusions.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-WSLDefenderExclusions.ps1:82 [PSAvoidUsingWriteHost] File 'Set-WSLDefenderExclusions.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Set-WSLDefenderExclusions.ps1:90 [PSAvoidUsingWriteHost] File 'Set-WSLDefenderExclusions.ps1' uses Write-Host. Avoid using Write-Host because it might not work in all hosts, does not work when there is no host, and (prior to PS 5.0) cannot be suppressed, captured, or redirected. Instead, use Write-Output, Write-Verbose, or Write-Information.
- Start-HVSockProxy.ps1:91 [PSAvoidAssignmentToAutomaticVariable] The Variable 'args' is an automatic variable that is built into PowerShell, assigning to it might have undesired side effects. If assignment is not by design, please use a different name.
- Stop-HVSockProxy.ps1:44 [PSAvoidUsingEmptyCatchBlock] Empty catch block is used. Please use Write-Error or throw statements in catch blocks.

## Error
- Network-Helpers.ps1:264 [PSAvoidAssignmentToAutomaticVariable] The Variable 'Host' cannot be assigned since it is a readonly automatic variable that is built into PowerShell, please use a different name.


